import React from 'react';
import styles from '../Css/2.module.css'

const Modules = () => {
    return (
        <div>
            <h1   className={styles.heading}>CSS Modules</h1>
        </div>
    );
};

export default Modules;